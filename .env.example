# Frontend environment example for React app
# Copy this to frontend/.env (or .env.local) and update before running dev server

# Base URL for API calls from the frontend. Do not include a trailing slash.
REACT_APP_API_URL=http://localhost:8080

# Optional: toggle analytics or other feature flags
REACT_APP_DISABLE_ANALYTICS=true
